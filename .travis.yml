language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t rajatsudagade/docker-react-tutorial -f Dockerfile.dev .

script:
  - docker run -e CI=true rajatsudagade/docker-react-tutorial npm run test

